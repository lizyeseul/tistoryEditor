<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="blog-list">
	<delete id="deleteBlogs">
    /*deleteBlogs*/
	DELETE FROM blog_list
	</delete>
	<insert id="insertBlogList">
	/*insertBlogList*/
	INSERT INTO main.blog_list (
		  BLOG_UUID
		, BLOG_ID
		, BLOG_NM
		, BLOG_TITLE
		, BLOG_URL
		, SCND_URL
		, NICK_NM
		, DESCP
		, DEFAULT_YN
		, ROLE_NM
		, BLOG_ICON_URL
		, FAVICON_URL
		, PROF_THMNL_IMG_URL
		, PROF_IMG_URL
		, STAT_POST
		, STAT_COMNT
		, STAT_TRKBK
		, STAT_GSTBOOK
		, STAT_INVTN
	) VALUES (
		#{blog_uuid},
		#{blogId},
		#{name},
		#{title},
		#{url},
		#{secondaryUrl},
		#{nickname},
		#{description},
		#{default},
		#{role},
		#{blogIconUrl},
		#{faviconUrl},
		#{profileThumbnailImageUrl},
		#{profileImageUrl},
		#{post},
		#{comment},
		#{trackback},
		#{guestbook},
		#{invitation}
	)
	</insert>



	<select id="selectBlogNm" resultType="string">
	/*selectBlogNm*/
	SELECT
		  BLOG_NM
	FROM  blog_list
	WHERE 1=1
	AND   BLOG_UUID = #{blog_uuid}
	</select>
	<select id="selectBlogList" resultType="map">
	/*selectBlogList*/
	SELECT
		  BLOG_UUID
		, BLOG_NM
	FROM  blog_list
	ORDER BY DEFAULT_YN DESC
	</select>
	<select id="selectBlogInfo" resultType="map">
	/*selectBlogInfo*/
	SELECT
		  BLOG_UUID
		, BLOG_ID
		, BLOG_NM
		, BLOG_TITLE
		, BLOG_URL
		, SCND_URL
		, NICK_NM
		, DESCP
		, DEFAULT_YN
		, ROLE_NM
		, BLOG_ICON_URL
		, FAVICON_URL
		, PROF_THMNL_IMG_URL
		, PROF_IMG_URL
		, STAT_POST
		, STAT_COMNT
		, STAT_TRKBK
		, STAT_GSTBOOK
		, STAT_INVTN
	FROM  blog_list
	WHERE 1=1
	AND   BLOG_UUID = #{blog_uuid}
	</select>
</mapper>